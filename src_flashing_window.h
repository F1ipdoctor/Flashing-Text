#ifndef FLASHING_WINDOW_H
#define FLASHING_WINDOW_H

#include <windows.h>

// Run the flashing window. Returns the Win32 message loop exit code.
int RunFlashingWindow(HINSTANCE hInstance, int nCmdShow);

#endif // FLASHING_WINDOW_H